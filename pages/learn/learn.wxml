<view class="container">
    <video id="myVideo"
           src="{{src}}"
           poster="{{poster}}"
           enable-play-gesture
           controls></video>
    <block wx:for="{{catalog}}" wx:key="index">
        <s-catalog data="{{item}}"></s-catalog>
    </block>
</view>
